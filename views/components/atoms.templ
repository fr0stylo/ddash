package components

templ MetaPill(label string, value string) {
	<span class="rounded-full border border-gray-200 bg-white px-2 py-1 text-xs text-gray-500">
		{ label } <span class="font-medium text-gray-700">{ value }</span>
	</span>
}
